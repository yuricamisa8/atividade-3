# Crie um programa que calcule a área de um retângulo, pedindo ao usuário a largura e a altura.

 
larga = int(input("qual a largura do retângulo"))

altu = int(input("qual a altura do retângulo"))

multiplicar = larga * altu

print(multiplicar)